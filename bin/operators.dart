void main() {

  // Basic math
  1 + 2 - 3 * 4 / 5;

  // Logic
  1 == 1; // is equal to
  1 < 2; // less than
  (1 >= 1) && ('a' == 'b'); // logical AND
  (1 >= 2) || ('a' == 'b'); // logical OR

  var x = 1;
  x++; // x = x + 1
  x--; // x = x - 1

  

}